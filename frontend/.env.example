# Public browser configuration. Use /api/v1 with the development proxy or an
# explicit backend URL such as http://localhost:8080/api/v1.
VITE_API_BASE_URL=/api/v1

# Development-only Vite proxy target. This value is not exposed to browser code.
DEV_PROXY_TARGET=http://localhost:8080
