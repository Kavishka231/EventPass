# EventPass frontend

This directory is reserved for the EventPass web application. No frontend framework has been selected or generated yet, so it intentionally contains only this workspace guide.

The frontend should consume the versioned backend API under `/api/v1` and must treat backend authorization and server-calculated pricing as authoritative.
